<UserControl x:Class="Kanstraction.ViewModels.StagePresetDesignerView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root"
             Background="{DynamicResource MaterialDesignPaper}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- content -->
            <RowDefinition Height="Auto"/>
            <!-- actions -->
        </Grid.RowDefinitions>

        <!-- ===== CONTENT AREA: either Empty State OR Editor ===== -->
        <Grid Grid.Row="0">
            <!-- EMPTY STATE -->
            <Border x:Name="EmptyStatePanel"
            Padding="24" Margin="12"
            Background="{DynamicResource MaterialDesignCardBackground}"
            BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8"
            Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource StagePresetDesignerView_NoPresetSelected}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="{DynamicResource StagePresetDesignerView_SelectPresetOrCreate}"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   TextAlignment="Center" Margin="0,0,0,16"/>
                    <Button Content="{DynamicResource StagePresetDesignerView_CreateNewPreset}" Click="EmptyStateCreateNew_Click"
                Style="{StaticResource MaterialDesignOutlinedButton}" Width="180" />
                </StackPanel>
            </Border>

            <!-- EDITOR -->
            <ScrollViewer x:Name="EditorPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- general -->
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <!-- sub-stages + materials -->
                    </Grid.RowDefinitions>

                    <!-- 1) GENERAL -->
                    <Border Grid.Row="0" Padding="12" Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="8">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDirty, ElementName=Root}" Value="True">
                                        <Setter Property="BorderBrush" Value="#E67E22"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="280"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <DockPanel Margin="0,0,0,8">
                                    <TextBlock Text="{DynamicResource StagePresetDesignerView_PresetName}" Width="120" VerticalAlignment="Center"/>
                                    <TextBox x:Name="TxtPresetName" Padding="8" TextChanged="TxtPresetName_TextChanged"
                         materialDesign:HintAssist.Hint="{DynamicResource Common_Required}"/>
                                </DockPanel>
                                <CheckBox x:Name="ChkActive" Content="{DynamicResource AdminHubView_Active}" Checked="ChkActive_Changed" Unchecked="ChkActive_Changed" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <TextBlock Text="{DynamicResource StagePresetDesignerView_Summary}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <TextBlock x:Name="TxtTotalSubs"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- 2) SUB-STAGES + 3) MATERIALS -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- SUB-STAGES -->
                        <Border Grid.Column="0" Padding="12" Background="{DynamicResource MaterialDesignCardBackground}"
                  BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{DynamicResource AdminHubView_SubStages}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                                <DataGrid Grid.Row="1" x:Name="SubStagesGrid" Margin="0,0,0,8"
                        AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="False"
                        HeadersVisibility="Column" GridLinesVisibility="None"
                        SelectionMode="Single"
                        SelectionChanged="SubStagesGrid_SelectionChanged"
                        PreparingCellForEdit="SubStagesGrid_PreparingCellForEdit"
                        CellEditEnding="SubStagesGrid_CellEditEnding"
                        Style="{StaticResource MaterialDesignDataGrid}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="#" Binding="{Binding OrderIndex}" Width="40" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="{DynamicResource AdminHubView_Name}" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                        <DataGridTemplateColumn Header="{DynamicResource AdminHubView_Order}" Width="120">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <Button Style="{StaticResource TinyIconButton}" Tag="{Binding}" Background="#3f51b5" Click="MoveSubStageUp_Click" Width="20" Height="20">
                                                            <iconPacks:PackIconFontAwesome Kind="ArrowUpSolid"   Foreground="White" Width="14" Height="14" />
                                                        </Button>
                                                        <Button Style="{StaticResource TinyIconButton}" Margin="2" Background="#3f51b5" Tag="{Binding}" Click="MoveSubStageDown_Click" Width="20" Height="20">
                                                            <iconPacks:PackIconFontAwesome Kind="ArrowDownSolid" Foreground="White" Width="14" Height="14" />
                                                        </Button>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="" Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Tag="{Binding}" Style="{StaticResource TinyIconButton}" Click="DeleteSubStage_Click">
                                                        <iconPacks:PackIconFontAwesome Kind="TrashCanRegular" Width="16" Height="16" Foreground="Red"/>
                                                    </Button>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="{DynamicResource StagePresetDesignerView_AddSubStage}" Click="AddSubStage_Click" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- MATERIALS (hidden until a sub-stage is selected/created) -->
                        <Border x:Name="MaterialsPanel" Grid.Column="2" Padding="12"
                  Background="{DynamicResource MaterialDesignCardBackground}"
                  BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8"
                  Visibility="Collapsed">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{DynamicResource StagePresetDesignerView_MaterialsForSelected}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                                <!-- Add material row -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                                    <ComboBox x:Name="CboMaterial" Width="280" Margin="0,0,8,0"
                          DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="MaterialSelected"/>
                                    <TextBox x:Name="TxtQty" Width="120" Margin="0,0,8,0"
                         materialDesign:HintAssist.Hint="{DynamicResource AddMaterialToSubStageDialog_QuantityHint}" />
                                    <TextBlock x:Name="TxtMTUnit" Width="30" Margin="8,0,8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                    <Button Content="{DynamicResource Common_Add}" Click="AddMaterial_Click" />
                                </StackPanel>

                                <DataGrid Grid.Row="2" x:Name="MaterialsGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                        CanUserSortColumns="False"
                        HeadersVisibility="Column" GridLinesVisibility="None"
                        Style="{StaticResource MaterialDesignDataGrid}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{DynamicResource OperationsView_MaterialHeader}" Binding="{Binding MaterialName}" IsReadOnly="True" Width="*"/>
                                        <DataGridTextColumn Header="{DynamicResource AdminHubView_Unit}" Binding="{Binding Unit}" IsReadOnly="True" Width="80"/>
                                        <DataGridTextColumn Header="{DynamicResource OperationsView_Qty}" Binding="{Binding Qty, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.##}}" Width="120"/>
                                        <DataGridTemplateColumn Header="" Width="80">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Style="{StaticResource TinyIconButton}" Tag="{Binding}" Click="RemoveMaterial_Click">
                                                        <iconPacks:PackIconFontAwesome Kind="TrashCanRegular" Width="16" Height="16" Foreground="Red"/>
                                                    </Button>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Grid>

        <!-- ACTIONS (only visible in editor mode) -->
        <DockPanel x:Name="ActionsPanel" Grid.Row="1" LastChildFill="False" Margin="12,6,12,12" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="{DynamicResource Common_Save}" Click="Save_Click" Margin="6,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                <Button Content="{DynamicResource Common_Cancel}" Click="Cancel_Click" Margin="6,0"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>